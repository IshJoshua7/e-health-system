version: 2
updates:
  - package-ecosystem: "composer"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    # Optionally ignore major version updates if you prefer manual control
    # ignore:
    #   - dependency-name: "php"
    #     update-types: ["version-update:semver-major"]
